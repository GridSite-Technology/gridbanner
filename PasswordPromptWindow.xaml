<Window x:Class="GridBanner.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter SSH Key Password"
        Width="400"
        Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="Your SSH private key is password protected." 
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="1" 
                   Name="KeyNameText"
                   Text="Enter the password to sign the challenge:"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>
        
        <PasswordBox Grid.Row="2" 
                     Name="PasswordInput"
                     VerticalAlignment="Top"
                     Height="30"
                     FontSize="14"/>
        
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Cancel" 
                    Width="80" 
                    Height="30" 
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button Content="OK" 
                    Width="80" 
                    Height="30"
                    IsDefault="True"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

